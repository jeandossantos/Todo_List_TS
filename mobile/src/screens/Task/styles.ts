import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    marginTop: 50,
    flex: 1,
    alignItems: 'center',
  },
  title: {
    fontSize: 50,
    textAlign: 'center',
    fontWeight: '700',
    color: '#fff',
  },
  contentList: {
    marginTop: 10,
  },
  nameTask: {
    color: '#000',
    backgroundColor: '#fff',
    marginBottom: 10,
    padding: 10,
    borderRadius: 8,
  },
});
